#include<stdio.h>
#include<stdlib.h>
void getmatrix(int **matrix,int rows,int cols);
void multiplymatrices(int **matrix1,int **matrix2,int **result,int r1,int c1,int c2);
#include"matrixhead.h"
void getmatrix(int **matrix,int rows,int cols)
{
   int i,j;
   for(i=0;i<rows;i++)
   {
      for(j=0;j<cols;j++)
      {
         scanf("%d",&matrix[i][j]);
      }

   }
}
void printmatrix(int **matrix,int rows,int cols)
{
   int i,j;
   for(i=0;i<rows;i++)
   {
      for(j=0;j<cols;j++)
      {
         printf("%d",matrix[i][j]);
      }
      printf("\n");
   }
}
void multiplymatrices(int **matrix1,int **matrix2,int **result,int r1,int c1,int c2)
{
   int i,j,k;
   for(i=0;i<r1;i++)
   {
      for(j=0;j<c2;j++)
      {
         result[i][j]=;
         for(k=0;k<c1;k++)
         {
         result[i][j]+=matrix1[i][j]*matrix2[k][j];
      }
   }
}
}
int main()
{
   int r1,r2,c1,c2,i,j,**matrix1,**matrix2,**result;
   printf("enter the number of rows and cols of the firstmatrix:");
   scanf("%d%d",&r1,&c1);
   printf("enter the numberof rows and cols of the secondmatrix:");
   scanf("%d%d",&r2,&c2);
   if(c1!=c2)
   {
      printf("matrix multiplication is not possible\n");
   }
   matrix1=(int**)malloc(r1*sizeof(int*));
   matrix2=(int**)malloc(r2*sizeof(int*));
   result=(int**)malloc(r1*sizeof(int*));
   for(i=0;i<r1;i++)
   {
      matrix1[i]=(int*)malloc(c1*sizeof(int));
      result[i]=(int*)malloc(c2*sizeof(int));
   }
   for(i=0;i<r2;i++)
   {
      matrix2[i]=(int*)malloc(c2*sizeof(int));
   }
   printf("enter the elements of first matrix:\n");
   getmatrix(matrix1,r1,c1);
   printf("enter the elements of second matrix:\n");
   getmatrix(matrix2,r2,c2);
   multiplymatrices(matrix1,matrix2,result,r1,c1,c2);
   printf("resultant matrix:\n");
   printmatrix(result,r1,c2);
   for(i=0;i<r1;i++)
   {
      free(matrix1[i]);
      free(result[i]);
   }
   for(i=0;i<r2;i++)
   {
      free(matrix2[i]);
   }
   free(matrix1);
   free(matrix2);
   free(result);
   return 0;
}

-------------------------------------------------------------------------------------------
